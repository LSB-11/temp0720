const lalalas = document.querySelectorAll(".lalala");
a = console.log(lalalas[0].textContent);
b = console.log(lalalas[1].textContent);
c = console.log(lalalas[2].textContent);

for (let i=0 ; i < 3; i++){
  lalalas[i].textContent = "NONO" 
}
// document.querySelectorAll(".lalala").textContent = "keke"
